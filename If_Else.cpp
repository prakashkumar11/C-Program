#include<iostream>
using namespace std;

int main()
{
    //<===================== if statement ===================>

    // if statement--> The if statement evaluates the condition inside the parentheses().
    // If the condition evaluates to true, the code inside the body of if is executed.
    // If the condition evaluates to false, the code inside the body of if is skipped. 

    /* Syntax --> if (condition) {

            body of if statements.

    } */

    // Example 1. For True statement
    // int number = 5;
    //condition is true then this code is executed
    // if (number > 0) {
    //     cout << number << endl; //true  
    // }

    // Example 2. For False stement
    // int number = 5;
    // // condition is False then code is not executed
    // if (number < 0) {
    //     cout << number << endl; // false
    // }

    // <========= Programming practice ==========>

    return 0;
}