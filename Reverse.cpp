#include<iostream>
using namespace std;

int main()
{
    // int n, reverse_number = 0, remainder;
    // cout <<"Enter a integer : ";
    // cin >> n;

    // while (n != 0) {
    //     remainder = n % 10;
    //     reverse_number = reverse_number * 10 + remainder;
    //     n = n/10;
    //     cout << n;
    // }

    // cout << "Reverse number = " << reverse_number;

    // int num, reverse_number = 0, remainder;
    // cout << "Enter a integer => ";
    // cin >> num;

    // while (num != 0) {
    //     remainder = num%10;
    //     reverse_number = reverse_number * 10 + remainder;
    //     num /= 10;
    // }

    // cout << " Reverse number => " << reverse_number;


    // <================= Reverse Number ===============>
    
    // int num, revNum=0, rem;
    // cout <<"Enter a integer number => ";
    // cin >> num;

    // while(num != 0) {
    //     rem = num%10;
    //     revNum = revNum * 10 + rem; 
    //     num /= 10;
    // }
    // cout << " Reverse Number => " <<revNum;

    // int num, reverse_number=0, remainder; 
    // cout <<"Enter integer number => ";
    // cin >>num;

    // while(num != 0 ) {
    //     remainder = num%10;
    //     reverse_number = reverse_number * 10 + remainder;
    //     num /= 10;
    // }

    // cout << "Reverse number => " << reverse_number;





    return 0;
}